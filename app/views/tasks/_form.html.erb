<%= simple_form_for task do |f| %>
    <%= f.input :title %>
    <%= f.input :details %>
    <% if task.new_record? == false %>
      <%= f.input :completed %>
    <% end %>
    <%= f.submit class: 'btn btn-primary' %>
  <% end %>
